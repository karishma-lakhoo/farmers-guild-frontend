{/*import React from 'react';
import {describe, it, expect, test} from '@jest/globals';
import { render, fireEvent } from '@testing-library/react-native';
import {Log_popup} from "../src/components/Log_popup";

// Mock AsyncStorage
jest.mock('@react-native-async-storage/async-storage', () => ({
    getItem: jest.fn(() => Promise.resolve('token')),
}));

describe('Log_popup', () => {
    test('renders correctly', () => {
        const { getByPlaceholderText, getByText } = render(<Log_popup />);
        expect(getByPlaceholderText('Garden Name')).toBeTruthy();
        expect(getByText('Add Garden')).toBeTruthy();
        expect(getByText('Close')).toBeTruthy();
    });


});   */}
